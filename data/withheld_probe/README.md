# Withheld-Probe Assets

The withheld-probe spectra, hidden labels, and sampling manifest are
intentionally omitted from the anonymous review package. This directory is a
placeholder documenting the expected asset location for controlled
post-publication release.

Reviewers can inspect the benchmark interface and scoring pipeline with
`data_split/public_task_data/` and `examples/hre_toy/`.
