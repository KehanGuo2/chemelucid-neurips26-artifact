# Private HRE Assets

The full chemistry-specific COMBO HRE files and expert reasoning graphs are
intentionally omitted from the anonymous review package. This directory is a
placeholder documenting the expected asset location only.

See `examples/hre_toy/` for sanitized examples that exercise the same scoring
interface without exposing the private grader layer.
